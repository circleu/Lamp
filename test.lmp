-- map := /\M :: (* -> *) -> * -> * ./\List :: * -> * .\x : M List Int .x;


_fact := \r : Int -> Int .\n : Int .If == n 0d0 Then 0d1 Else * n (r (- n 0d1));
fact := Fix _fact;

-- test ::= (\F :: * -> * .\X :: * .F (F X)) (\Y :: * .Y -> Y) Int;


-- Global Mut gdt = 0d1;
-- gdt = 0d2;

-- _test := \x : Int .x;
-- test := Fix _test;

Global Mut gdt = Alloc 0d23;
Let asdf = gdt In Peek Int asdf;

test := /\A :: * -> * .\x : A Int .x;
list ::= \x :: * .x;

(test[list])(0d1);

fact 0d5;